<div class="card">
    <div class="card-body">
        <p class="fs-4"><span class="{{ $icon ?? 'fas fa-question' }} px-1"></span>{{ $title ?? 'NULL' }}</p>
        <p class="fw-bold fs-3">{{ $data ?? 0 }}</p>
    </div>
</div>
